[pytest]
testpaths = tests
markers =
    integration: requires `docker compose up -d` + a registered Debezium connector (see scripts/register-debezium-connector.sh)
    e2e: requires the full local stack (docker-compose) AND a running Django server (manage.py runserver)
addopts = -m "not integration and not e2e"
